@startuml

!include ../screens.puml

(*) --> [User has uploaded a dataset and \n has selected a dataset]"
{{
    $view_dataset_page("Ontario Lab.csv", 0)
}}
" as view_dataset_page

view_dataset_page --> [User clicks on the **Validate Dataset** button]"
{{
    $validation_setup_dialog()
}}
" as validation_setup_dialog

validation_setup_dialog --> [User clicks on the **Start Validation** button]"
{{
    $validation_progress_dialog()
}}
" as validation_progress_dialog

validation_setup_dialog --> [User clicks on the **Cancel** button] view_dataset_page

validation_progress_dialog --> [Validation has completed]"
{{
    $validation_summary_page()
}}
" as validation_summary_page

validation_progress_dialog --> [User clicks on the **Cancel** button]"
{{
    $cancel_validation_dialog()
}}
" as cancel_validation_dialog

cancel_validation_dialog --> [User clicks on the **Yes** button] view_dataset_page

cancel_validation_dialog --> [User clicks on the **No** button] validation_progress_dialog

@enduml
