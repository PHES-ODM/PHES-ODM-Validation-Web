@startuml

!include ../screens.puml

(*) --> [User has uploaded a dataset and \n has selected a dataset]"
{{
    $view_dataset_page("Ontario Lab.csv", 0)
}}
" as view_dataset_page

view_dataset_page --> [User clicks on the **Validate Dataset** button]"
{{
    $new_validation_dialog()
}}
" as new_validation_dialog

new_validation_dialog --> [User clicks on the **Start Validation** button]"
{{
    $validating_dialog()
}}
" as validating_dialog

new_validation_dialog --> [User clicks on the **Cancel** button] view_dataset_page

validating_dialog --> [Validation has completed]"
{{
    $validation_summary_page()
}}
" as validation_summary_page

validating_dialog --> [User clicks on the **Cancel** button]"
{{
    $cancel_validation_dialog()
}}
" as cancel_validation_dialog

cancel_validation_dialog --> [User clicks on the **Yes** button] view_dataset_page

cancel_validation_dialog --> [User clicks on the **No** button] validating_dialog

@enduml
